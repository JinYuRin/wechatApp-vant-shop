/* components/category-grid/index.wxss */
.container {
    width: 100%;
    height: 320rpx;
/*采用flex布局？？有空看看    */
    display: flex;
    flex-direction: row;
    /*row是横向布局，column是纵向布局*/
    align-items: center;
    /*内含有的items的水平方向的居中*/
    justify-content: center;
/*    justify是法是正义，代表垂直方向居中*/
    background-color: #FFFFFF;
}
.inner-container{
    width: 730rpx;
    height: 300rpx;
    border: 1px dashed #DCEBE6;
/* border dashed 边界 虚线 */
}
.grid-item{
    width: 200rpx;
    /*不设置宽会导致点击区域过小，虽然不影响视觉，lin-ui也会自动调整 只要不超过一定的数值，你会算的啊*/
    /*height: 200rpx;根本不需要设置高？lin-ui能自动根据内容调整成内容物集合的总高度
    ？？那瀑布流也很牛逼啊*/
    display: flex;
    flex-direction: column;
    /*主轴是自上向下垂直，所以align-items交叉轴上的对齐就是水平方向的对齐为居中对齐*/
    align-items: center;
}
.img{
    width:60rpx;
    height: 60rpx;
}
.text{
    margin-top: 6rpx;
    /*向上的间距*/
    font-size: 24rpx;
    color: #333333;
}