<view class="container">
    <title class="title">{{fence.title}}</title>
    <view class="row-container">
        <block wx:for="{{fence.cells}}" wx:key="{{index}}">
            <s-cell x="{{x}}" y="{{index}}" class="cell" cell="{{item}}"></s-cell>
        </block>
    </view>
    <view class="hr">
    </view>
</view>